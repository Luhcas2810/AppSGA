
@{
    ViewBag.Title = "Crear";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<!-- Usuario/crear -->
<p>

</p>
<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="#">Administración</a></li>
        <li class="breadcrumb-item active" aria-current="page">Usuarios</li>
    </ol>
</nav>
<div class="row">
    <div class="col-sm-12">
        <div class="card">
            <div class="card-header bg-dark text-white">Lista de Usuarios</div>
            <div class="card-body">
                <button type="button" class="btn btn-sm btn-outline-success" onclick="abrirPopUpFormUsuario(null)">
                    <i class="fa fa-plus" aria-hidden="true"></i> Agregar Nuevo
                </button>
                <hr />
                <table id="tbUsuario" class="table table-striped table-bordered nowrap compact" style="width:100%">
                    <thead>
                        <tr>
                            <th>Usuario</th>
                            <th>Rol</th>
                            <th>Estado</th>
                            <th>Acciones</th>
                        </tr>
                    </thead>
                    <tbody></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="FormModal" tabindex="-1" role="dialog" aria-labelledby="FormModalLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header bg-dark text-white">
                <h5 class="modal-title" id="FormModalLabel">Usuario</h5>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <form id="formUsuario">
                    <input type="hidden" id="txtIdUsuario" value="0" />
                    <div class="form-group">
                        <label for="txtUsuario">Usuario</label>
                        <input type="text" class="form-control" id="txtUsuario" />
                    </div>
                    <div class="form-group">
                        <label for="txtContrasenia">Contraseña</label>
                        <input type="password" class="form-control" id="txtContrasenia" />
                    </div>
                    <div class="form-group">
                        <label for="cboRol">Rol</label>
                        <select class="form-control" id="cboRol"></select>
                    </div>
                    <div class="form-group">
                        <label for="cboEstado">Estado</label>
                        <select class="form-control" id="cboEstado">
                            <option value="1">Activo</option>
                            <option value="0">No Activo</option>
                        </select>
                    </div>
                </form>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-danger" data-dismiss="modal">Cerrar</button>
                <button type="button" class="btn btn-primary" onclick="GuardarUsuario()">Guardar</button>
            </div>
        </div>
    </div>
</div>
@*<table id="tbUsuario" class="table table-bordered">
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Rol</th>
                <th>Estado</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        </tbody>
    </table>*@

@section scripts{
    <script src="@Url.Content("~/Scripts/Views/Usuario_Crear.js")" type="text/javascript"></script>
    <script type="text/javascript">
        var getListaUsuarioURL = '@Url.Action("ObtenerListaUsuario", "Usuario")';
        @*var postActualizarUsuarioURL = '@Url.Action("ActualizarUsuario", "Usuario")';*@
         var postCrearUsuarioURL = '@Url.Action("CrearUsuario", "Usuario")';
        var getListaRolURL = '@Url.Action("ObtenerListaRol", "Rol")';
    </script>
}